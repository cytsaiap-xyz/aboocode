Compact the current conversation to free up context. Summarize everything so far into a single dense assistant turn and then continue.

$ARGUMENTS

Steps:
1. Read the recent messages in this session.
2. Identify: the high-level user goal, what has already been done, what remains, key decisions and their rationale, any open questions or blockers.
3. Produce a compact summary in markdown with these sections:
   - **Goal**: one-sentence restatement of what the user wants
   - **Done**: bullet list of completed sub-tasks (file paths + what changed)
   - **Remaining**: bullet list of what's still to do
   - **Decisions**: any non-obvious choices made, with why
   - **Open questions**: anything waiting on the user
4. Do NOT re-derive things already visible in the current project state (file structure, dependency versions, etc.). Those can be re-read; the compact should only preserve the non-derivable reasoning trail.
5. Keep the compact under 2000 tokens.
