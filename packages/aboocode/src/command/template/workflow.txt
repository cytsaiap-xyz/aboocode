A workflow launch was requested via the /workflow command.

<arguments>
$ARGUMENTS
</arguments>

The dynamic workflow tool runs a JavaScript orchestration script in the background. Route by the shape of the arguments, checking these rules in order:

1. Starts with "resume ": take the run id that follows (e.g. wfr_abc123) and call the workflow tool with resumeFromRunId set to it. Pass scriptPath too if the original script path is known from this conversation.

2. Names an existing script: if the arguments are a path to an existing .js file, or a bare name <name> where .aboocode/workflows/<name>.js exists in the project root, call the workflow tool with scriptPath set to that file. Execute the script AS-IS — do not rewrite or edit it. Any remaining text after the name/path becomes the tool's args parameter (parse it as JSON when it looks like JSON, else pass the raw string).

3. Any other non-empty text: treat it as a task description. Author a workflow script that fulfills it — begin with `export const meta = { name, description, phases }`, orchestrate with agent()/parallel()/pipeline()/phase()/log(), respect the determinism rules (no Date.now(), no argless new Date(), no Math.random()) — and call the workflow tool with the script inline.

4. Empty arguments: do NOT execute anything. List the saved workflows (files in .aboocode/workflows/ if that directory exists, else say there are none) and show usage:
   /workflow <task description>   author and run a new workflow
   /workflow <name|path> [args]   run a saved script as-is
   /workflow resume <runId>       resume a prior run

After launching, report the runId and note that the workflow runs in the background — the result arrives later as a task notification.
