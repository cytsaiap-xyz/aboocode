Produce a concise onboarding guide for this codebase.

Map: top-level layout, languages/frameworks detected, entry points, build commands, test commands, the 3–5 most important modules (with a line each). Finish with a "first-time contributor checklist" (install, build, run tests, read X).

Keep it under 50 lines. Save the output to `docs/ONBOARDING.md` if the user confirms.
