---
title: CLI
description: Opciones y comandos de la CLI de Aboocode.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI de Aboocode de forma predeterminada inicia el [TUI](/docs/tui) cuando se ejecuta sin ningún argumento.

```bash
aboocode
```

Pero también acepta comandos como se documenta en esta página. Esto le permite interactuar con Aboocode mediante programación.

```bash
aboocode run "Explain how closures work in JavaScript"
```

---

### tui

Inicie la interfaz de usuario del terminal Aboocode.

```bash
aboocode [project]
```

#### Opciones

| Opción       | Corta | Descripción                                                           |
| ------------ | ----- | --------------------------------------------------------------------- |
| `--continue` | `-c`  | Continuar la última sesión                                            |
| `--session`  | `-s`  | ID de sesión para continuar                                           |
| `--fork`     |       | Bifurcar la sesión al continuar (usar con `--continue` o `--session`) |
| `--prompt`   |       | Aviso de uso                                                          |
| `--model`    | `-m`  | Modelo a utilizar en forma de proveedor/modelo                        |
| `--agent`    |       | Agente a utilizar                                                     |
| `--port`     |       | Puerto para escuchar                                                  |
| `--hostname` |       | Nombre de host para escuchar                                          |

---

## Comandos

La CLI de Aboocode también tiene los siguientes comandos.

---

### agent

Administrar agentes para Aboocode.

```bash
aboocode agent [command]
```

---

### attach

Conecte una terminal a un servidor backend Aboocode que ya se esté ejecutando y iniciado mediante los comandos `serve` o `web`.

```bash
aboocode attach [url]
```

Esto permite usar TUI con un backend remoto Aboocode. Por ejemplo:

```bash
# Start the backend server for web/mobile access
aboocode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
aboocode attach http://10.20.30.40:4096
```

#### Opciones

| Opción      | Corta | Descripción                               |
| ----------- | ----- | ----------------------------------------- |
| `--dir`     |       | Directorio de trabajo para iniciar TUI en |
| `--session` | `-s`  | ID de sesión para continuar               |

---

#### create

Cree un nuevo agente con configuración personalizada.

```bash
aboocode agent create
```

Este comando lo guiará en la creación de un nuevo agente con un mensaje del sistema personalizado y una configuración de herramientas.

---

#### list

Enumere todos los agentes disponibles.

```bash
aboocode agent list
```

---

### auth

Comando para administrar credenciales e iniciar sesión para proveedores.

```bash
aboocode auth [command]
```

---

#### login

Aboocode funciona con la lista de proveedores en [Models.dev](https://models.dev), por lo que puede usar `aboocode auth login` para configurar las claves API para cualquier proveedor que desee utilizar. Esto se almacena en `~/.local/share/aboocode/auth.json`.

```bash
aboocode auth login
```

Cuando se inicia Aboocode, carga los proveedores desde el archivo de credenciales. Y si hay claves definidas en sus entornos o un archivo `.env` en su proyecto.

---

#### list

Enumera todos los proveedores autenticados tal como están almacenados en el archivo de credenciales.

```bash
aboocode auth list
```

O la versión corta.

```bash
aboocode auth ls
```

---

#### logout

Cierra tu sesión de un proveedor eliminándolo del archivo de credenciales.

```bash
aboocode auth logout
```

---

### github

Administre el agente GitHub para la automatización del repositorio.

```bash
aboocode github [command]
```

---

#### install

Instale el agente GitHub en su repositorio.

```bash
aboocode github install
```

Esto configura el flujo de trabajo de acciones GitHub necesario y lo guía a través del proceso de configuración. [Más información](/docs/github).

---

#### run

Ejecute el agente GitHub. Esto se usa normalmente en acciones GitHub.

```bash
aboocode github run
```

##### Opciones

| Opción    | Descripción                                    |
| --------- | ---------------------------------------------- |
| `--event` | GitHub evento simulado para ejecutar el agente |
| `--token` | GitHub token de acceso personal                |

---

### mcp

Administrar servidores de protocolo de contexto modelo.

```bash
aboocode mcp [command]
```

---

#### add

Agregue un servidor MCP a su configuración.

```bash
aboocode mcp add
```

Este comando lo guiará para agregar un servidor MCP local o remoto.

---

#### list

Enumere todos los servidores MCP configurados y su estado de conexión.

```bash
aboocode mcp list
```

O utilice la versión corta.

```bash
aboocode mcp ls
```

---

#### auth

Autentíquese con un servidor MCP habilitado para OAuth.

```bash
aboocode mcp auth [name]
```

Si no proporciona un nombre de servidor, se le pedirá que seleccione entre los servidores compatibles con OAuth disponibles.

También puede enumerar los servidores compatibles con OAuth y su estado de autenticación.

```bash
aboocode mcp auth list
```

O utilice la versión corta.

```bash
aboocode mcp auth ls
```

---

#### logout

Elimine las credenciales OAuth para un servidor MCP.

```bash
aboocode mcp logout [name]
```

---

#### debug

Depurar problemas de conexión OAuth para un servidor MCP.

```bash
aboocode mcp debug <name>
```

---

### models

Enumere todos los modelos disponibles de los proveedores configurados.

```bash
aboocode models [provider]
```

Este comando muestra todos los modelos disponibles en sus proveedores configurados en el formato `provider/model`.

Esto es útil para determinar el nombre exacto del modelo que se usará en [su configuración](/docs/config/).

Opcionalmente, puede pasar un ID de proveedor para filtrar modelos por ese proveedor.

```bash
aboocode models anthropic
```

#### Opciones

| Opción      | Descripción                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--refresh` | Actualizar la caché de modelos desde models.dev                             |
| `--verbose` | Utilice una salida del modelo más detallada (incluye metadatos como costos) |

Utilice el indicador `--refresh` para actualizar la lista de modelos almacenados en caché. Esto es útil cuando se han agregado nuevos modelos a un proveedor y desea verlos en Aboocode.

```bash
aboocode models --refresh
```

---

### run

Ejecute aboocode en modo no interactivo pasando un mensaje directamente.

```bash
aboocode run [message..]
```

Esto es útil para secuencias de comandos, automatización o cuando desea una respuesta rápida sin iniciar el TUI completo. Por ejemplo.

```bash "aboocode run"
aboocode run Explain the use of context in Go
```

También puede conectarse a una instancia `aboocode serve` en ejecución para evitar tiempos de arranque en frío del servidor MCP en cada ejecución:

```bash
# Start a headless server in one terminal
aboocode serve

# In another terminal, run commands that attach to it
aboocode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opciones

| Opción       | Corta | Descripción                                                                         |
| ------------ | ----- | ----------------------------------------------------------------------------------- |
| `--command`  |       | El comando a ejecutar, use mensaje para args                                        |
| `--continue` | `-c`  | Continuar la última sesión                                                          |
| `--session`  | `-s`  | ID de sesión para continuar                                                         |
| `--fork`     |       | Bifurcar la sesión al continuar (usar con `--continue` o `--session`)               |
| `--share`    |       | Comparte la sesión                                                                  |
| `--model`    | `-m`  | Modelo a utilizar en forma de proveedor/modelo                                      |
| `--agent`    |       | Agente a utilizar                                                                   |
| `--file`     | `-f`  | Archivo(s) para adjuntar al mensaje                                                 |
| `--format`   |       | Formato: predeterminado (formateado) o json (eventos JSON sin formato)              |
| `--title`    |       | Título de la sesión (utiliza un mensaje truncado si no se proporciona ningún valor) |
| `--attach`   |       | Adjuntar a un servidor aboocode en ejecución (por ejemplo, http://localhost:4096)   |
| `--port`     |       | Puerto para el servidor local (el puerto predeterminado es aleatorio)               |

---

### serve

Inicie un servidor Aboocode sin cabeza para acceso API. Consulte los [documentos del servidor](/docs/server) para conocer la interfaz HTTP completa.

```bash
aboocode serve
```

Esto inicia un servidor HTTP que proporciona acceso API a la funcionalidad aboocode sin la interfaz TUI. Configure `ABOOCODE_SERVER_PASSWORD` para habilitar la autenticación básica HTTP (el nombre de usuario predeterminado es `aboocode`).

#### Opciones

| Opción       | Descripción                                          |
| ------------ | ---------------------------------------------------- |
| `--port`     | Puerto para escuchar                                 |
| `--hostname` | Nombre de host para escuchar                         |
| `--mdns`     | Habilitar el descubrimiento de mDNS                  |
| `--cors`     | Orígenes de navegador adicionales para permitir CORS |

---

### session

Administrar Aboocode sesiones.

```bash
aboocode session [command]
```

---

#### list

Enumere todas las sesiones Aboocode.

```bash
aboocode session list
```

##### Opciones

| Opción        | Corta | Descripción                             |
| ------------- | ----- | --------------------------------------- |
| `--max-count` | `-n`  | Limitar a N sesiones más recientes      |
| `--format`    |       | Formato de salida: tabla o json (tabla) |

---

### stats

Muestre el uso de tokens y las estadísticas de costos para sus sesiones Aboocode.

```bash
aboocode stats
```

#### Opciones

| Opción      | Descripción                                                                                                              |
| ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| `--days`    | Mostrar estadísticas de los últimos N días (todo el tiempo)                                                              |
| `--tools`   | Número de herramientas para mostrar (todas)                                                                              |
| `--models`  | Mostrar el desglose del uso del modelo (oculto de forma predeterminada). Pase un número para mostrar la parte superior N |
| `--project` | Filtrar por proyecto (todos los proyectos, cadena vacía: proyecto actual)                                                |

---

### export

Exportar datos de la sesión como JSON.

```bash
aboocode export [sessionID]
```

Si no proporciona una ID de sesión, se le pedirá que seleccione entre las sesiones disponibles.

---

### import

Importe datos de sesión desde un archivo JSON o una URL compartida Aboocode.

```bash
aboocode import <file>
```

Puede importar desde un archivo local o una URL compartida Aboocode.

```bash
aboocode import session.json
aboocode import https://opncd.ai/s/abc123
```

---

### web

Inicie un servidor Aboocode sin cabeza con una interfaz web.

```bash
aboocode web
```

Esto inicia un servidor HTTP y abre un navegador web para acceder a Aboocode a través de una interfaz web. Configure `ABOOCODE_SERVER_PASSWORD` para habilitar la autenticación básica HTTP (el nombre de usuario predeterminado es `aboocode`).

#### Opciones

| Opción       | Descripción                                          |
| ------------ | ---------------------------------------------------- |
| `--port`     | Puerto para escuchar                                 |
| `--hostname` | Nombre de host para escuchar                         |
| `--mdns`     | Habilitar el descubrimiento de mDNS                  |
| `--cors`     | Orígenes de navegador adicionales para permitir CORS |

---

### acp

Inicie un servidor ACP (Agent Client Protocol).

```bash
aboocode acp
```

Este comando inicia un servidor ACP que se comunica a través de stdin/stdout usando nd-JSON.

#### Opciones

| Opción       | Descripción                  |
| ------------ | ---------------------------- |
| `--cwd`      | Directorio de trabajo        |
| `--port`     | Puerto para escuchar         |
| `--hostname` | Nombre de host para escuchar |

---

### uninstall

Desinstale Aboocode y elimine todos los archivos relacionados.

```bash
aboocode uninstall
```

#### Opciones

| Opción          | Corta | Descripción                               |
| --------------- | ----- | ----------------------------------------- |
| `--keep-config` | `-c`  | Mantener archivos de configuración        |
| `--keep-data`   | `-d`  | Conservar datos de sesión e instantáneas  |
| `--dry-run`     |       | Mostrar lo que se eliminaría sin eliminar |
| `--force`       | `-f`  | Saltar mensajes de confirmación           |

---

### upgrade

Actualiza aboocode a la última versión o a una versión específica.

```bash
aboocode upgrade [target]
```

Para actualizar a la última versión.

```bash
aboocode upgrade
```

Para actualizar a una versión específica.

```bash
aboocode upgrade v0.1.48
```

#### Opciones

| Opción     | Corta | Descripción                                                         |
| ---------- | ----- | ------------------------------------------------------------------- |
| `--method` | `-m`  | El método de instalación que se utilizó; curl, npm, pnpm, bun, brew |

---

## Opciones globales

La CLI de Aboocode toma las siguientes banderas globales.

| Opción         | Corta | Descripción                                  |
| -------------- | ----- | -------------------------------------------- |
| `--help`       | `-h`  | Mostrar ayuda                                |
| `--version`    | `-v`  | Número de versión de impresión               |
| `--print-logs` |       | Imprimir registros en stderr                 |
| `--log-level`  |       | Nivel de registro (DEBUG, INFO, WARN, ERROR) |

---

## Variables de entorno

Aboocode se puede configurar mediante variables de entorno.

| Variable                              | Type     | Description                                                                     |
| ------------------------------------- | -------- | ------------------------------------------------------------------------------- |
| `ABOOCODE_AUTO_SHARE`                 | booleano | Compartir sesiones automáticamente                                              |
| `ABOOCODE_GIT_BASH_PATH`              | cadena   | Ruta al ejecutable de Git Bash en Windows                                       |
| `ABOOCODE_CONFIG`                     | cadena   | Ruta al archivo de configuración                                                |
| `ABOOCODE_CONFIG_DIR`                 | cadena   | Ruta al directorio de configuración                                             |
| `ABOOCODE_CONFIG_CONTENT`             | cadena   | Contenido de configuración json en línea                                        |
| `ABOOCODE_DISABLE_AUTOUPDATE`         | booleano | Deshabilitar las comprobaciones automáticas de actualizaciones                  |
| `ABOOCODE_DISABLE_PRUNE`              | booleano | Deshabilitar la poda de datos antiguos                                          |
| `ABOOCODE_DISABLE_TERMINAL_TITLE`     | booleano | Deshabilitar las actualizaciones automáticas de títulos de terminal             |
| `ABOOCODE_PERMISSION`                 | cadena   | Configuración de permisos json incorporados                                     |
| `ABOOCODE_DISABLE_DEFAULT_PLUGINS`    | booleano | Deshabilitar complementos predeterminados                                       |
| `ABOOCODE_DISABLE_LSP_DOWNLOAD`       | booleano | Deshabilitar las descargas automáticas del servidor LSP                         |
| `ABOOCODE_ENABLE_EXPERIMENTAL_MODELS` | booleano | Habilitar modelos experimentales                                                |
| `ABOOCODE_DISABLE_AUTOCOMPACT`        | booleano | Deshabilitar la compactación automática de contexto                             |
| `ABOOCODE_DISABLE_CLAUDE_CODE`        | booleano | Deshabilitar la lectura desde `.claude` (mensaje + habilidades)                 |
| `ABOOCODE_DISABLE_CLAUDE_CODE_PROMPT` | booleano | Desactivar lectura `~/.claude/CLAUDE.md`                                        |
| `ABOOCODE_DISABLE_CLAUDE_CODE_SKILLS` | booleano | Deshabilitar la carga `.claude/skills`                                          |
| `ABOOCODE_DISABLE_MODELS_FETCH`       | booleano | Deshabilitar la recuperación de modelos desde fuentes remotas                   |
| `ABOOCODE_FAKE_VCS`                   | cadena   | Proveedor de VCS falso para fines de prueba                                     |
| `ABOOCODE_DISABLE_FILETIME_CHECK`     | booleano | Deshabilite la verificación del tiempo del archivo para optimizarlo             |
| `ABOOCODE_CLIENT`                     | cadena   | Identificador de cliente (por defecto `cli`)                                    |
| `ABOOCODE_ENABLE_EXA`                 | booleano | Habilitar las herramientas de búsqueda web de Exa                               |
| `ABOOCODE_SERVER_PASSWORD`            | cadena   | Habilite la autenticación básica para `serve`/`web`                             |
| `ABOOCODE_SERVER_USERNAME`            | cadena   | Anular el nombre de usuario de autenticación básica (predeterminado `aboocode`) |
| `ABOOCODE_MODELS_URL`                 | cadena   | URL personalizada para buscar la configuración de modelos                       |

---

### Experimental

Estas variables de entorno habilitan funciones experimentales que pueden cambiar o eliminarse.

| Variable                                        | Type     | Description                                                |
| ----------------------------------------------- | -------- | ---------------------------------------------------------- |
| `ABOOCODE_EXPERIMENTAL`                         | booleano | Habilitar todas las funciones experimentales               |
| `ABOOCODE_EXPERIMENTAL_ICON_DISCOVERY`          | booleano | Habilitar descubrimiento de íconos                         |
| `ABOOCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | booleano | Deshabilitar copia al seleccionar en TUI                   |
| `ABOOCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | número   | Tiempo de espera predeterminado para comandos bash en ms   |
| `ABOOCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | número   | Tokens de salida máximos para respuestas LLM               |
| `ABOOCODE_EXPERIMENTAL_FILEWATCHER`             | booleano | Habilite el observador de archivos para todo el directorio |
| `ABOOCODE_EXPERIMENTAL_OXFMT`                   | booleano | Habilitar el formateador oxfmt                             |
| `ABOOCODE_EXPERIMENTAL_LSP_TOOL`                | booleano | Habilitar herramienta experimental LSP                     |
| `ABOOCODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | booleano | Deshabilitar el observador de archivos                     |
| `ABOOCODE_EXPERIMENTAL_EXA`                     | booleano | Habilitar funciones experimentales de Exa                  |
| `ABOOCODE_EXPERIMENTAL_LSP_TY`                  | booleano | Habilitar la verificación de tipo experimental LSP         |
| `ABOOCODE_EXPERIMENTAL_MARKDOWN`                | booleano | Habilitar funciones de Markdown experimentales             |
| `ABOOCODE_EXPERIMENTAL_PLAN_MODE`               | booleano | Habilitar modo de plan                                     |
