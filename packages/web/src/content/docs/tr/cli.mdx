---
title: CLI
description: aboocode CLI seçenekleri ve komutları.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

aboocode CLI herhangi bir argüman olmadan çalıştırıldığında varsayılan olarak [TUI](/docs/tui)'yi başlatır.

```bash
aboocode
```

Ancak bu sayfada belgelendiği gibi komutları da kabul eder. Bu, aboocode ile programlı olarak etkileşim kurmanıza olanak tanır.

```bash
aboocode run "Explain how closures work in JavaScript"
```

---

### tui

aboocode terminal kullanıcı arayüzünü başlatır.

```bash
aboocode [project]
```

#### Bayraklar

| Bayrak       | Kısa | Açıklama                                                                    |
| ------------ | ---- | --------------------------------------------------------------------------- |
| `--continue` | `-c` | Son oturuma devam et                                                        |
| `--session`  | `-s` | Devam edecek oturum kimliği                                                 |
| `--fork`     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın) |
| `--prompt`   |      | Kullanılacak prompt                                                         |
| `--model`    | `-m` | provider/model biçiminde kullanılacak model                                 |
| `--agent`    |      | Kullanılacak agent                                                          |
| `--port`     |      | Dinlenecek port                                                             |
| `--hostname` |      | Dinlenecek host adı                                                         |

---

## Komutlar

aboocode CLI ayrıca aşağıdaki komutlara da sahiptir.

---

### agent

aboocode için agent'ları yönetin.

```bash
aboocode agent [command]
```

---

### attach

`serve` veya `web` komutlarıyla başlatılan, halihazırda çalışan bir aboocode arka uç sunucusuna bir terminal ekleyin.

```bash
aboocode attach [url]
```

Bu, TUI öğesinin uzak bir aboocode arka ucuyla kullanılmasına olanak tanır. Örneğin:

```bash
# Start the backend server for web/mobile access
aboocode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
aboocode attach http://10.20.30.40:4096
```

#### Bayraklar

| Bayrak      | Kısa | Tanım                                |
| ----------- | ---- | ------------------------------------ |
| `--dir`     |      | TUI'yi başlatmak için çalışma dizini |
| `--session` | `-s` | Devam edecek oturum açma bilgileri   |

---

#### create

Özel yapılandırmaya sahip yeni bir agent oluşturun.

```bash
aboocode agent create
```

Bu komut, özel sistem istemi ve araç yapılandırmasıyla yeni bir agent oluşturma konusunda size yol gösterecektir.

---

#### list

Mevcut tüm agent'ları listeleyin.

```bash
aboocode agent list
```

---

### auth

Sağlayıcılar için kimlik bilgilerini ve oturum açmayı yönetme komutu.

```bash
aboocode auth [command]
```

---

#### login

aboocode, [Models.dev](https://models.dev) adresindeki sağlayıcı listesi tarafından desteklenmektedir, dolayısıyla kullanmak istediğiniz herhangi bir sağlayıcı için API anahtarlarını ayarlamak üzere `aboocode auth login` kullanabilirsiniz. Bu `~/.local/share/aboocode/auth.json`'de saklanıyor.

```bash
aboocode auth login
```

aboocode başlatıldığında sağlayıcıları kimlik bilgileri dosyasından yükler. Ayrıca ortam değişkenlerindeki veya projenizdeki `.env` dosyasındaki anahtarları da kullanır.

---

#### list

Kimlik bilgileri dosyasında depolanan tüm kimliği doğrulanmış sağlayıcıları listeler.

```bash
aboocode auth list
```

Veya kısa versiyonu.

```bash
aboocode auth ls
```

---

#### logout

Bir sağlayıcıyı kimlik bilgileri dosyasından temizleyerek oturumunuzu kapatır.

```bash
aboocode auth logout
```

---

### github

Depo otomasyonu için GitHub aracısını yönetin.

```bash
aboocode github [command]
```

---

#### install

GitHub aracısını deponuza yükleyin.

```bash
aboocode github install
```

Bu komut gerekli GitHub Actions workflow'unu kurar ve yapılandırma adımlarında size rehberlik eder. [Daha fazla bilgi](/docs/github).

---

#### run

GitHub aracısını çalıştırın. Bu genellikle GitHub Eylemlerinde kullanılır.

```bash
aboocode github run
```

##### Bayraklar

| Bayrak    | Açıklama                                    |
| --------- | ------------------------------------------- |
| `--event` | Aracıyı çalıştırmak için GitHub sahte olayı |
| `--token` | GitHub personal access token                |

---

### mcp

Model Bağlam Protokolü sunucularını yönetin.

```bash
aboocode mcp [command]
```

---

#### add

Yapılandırmanıza bir MCP sunucusu ekleyin.

```bash
aboocode mcp add
```

Bu komut, yerel veya uzak bir MCP sunucusu ekleme konusunda size yol gösterecektir.

---

#### list

Yapılandırılmış tüm MCP sunucularını ve bağlantı durumlarını listeleyin.

```bash
aboocode mcp list
```

Veya kısa versiyonunu kullanın.

```bash
aboocode mcp ls
```

---

#### auth

OAuth'un etkin olduğu bir MCP sunucusuyla kimlik doğrulaması yapın.

```bash
aboocode mcp auth [name]
```

Sunucu adı belirtmezseniz mevcut OAuth özellikli sunucular arasından seçim yapmanız istenir.

Ayrıca OAuth özellikli sunucuları ve bunların kimlik doğrulama durumlarını da listeleyebilirsiniz.

```bash
aboocode mcp auth list
```

Veya kısa versiyonunu kullanın.

```bash
aboocode mcp auth ls
```

---

#### logout

MCP sunucusu için OAuth kimlik bilgilerini kaldırın.

```bash
aboocode mcp logout [name]
```

---

#### debug

MCP sunucusu için OAuth bağlantı sorunlarının hatalarını ayıklayın.

```bash
aboocode mcp debug <name>
```

---

### models

Yapılandırılmış sağlayıcıların tüm mevcut modellerini listeleyin.

```bash
aboocode models [provider]
```

Bu komut, yapılandırdığınız sağlayıcılardaki modelleri `provider/model` formatında listeler.

Bu, [config dosyanızda](/docs/config/) kullanmanız gereken tam model adını bulmak için kullanışlıdır.

Modelleri bu sağlayıcıya göre filtrelemek için isteğe bağlı olarak bir sağlayıcı kimliğini iletebilirsiniz.

```bash
aboocode models anthropic
```

#### Bayraklar

| Bayrak      | Tanım                                                                       |
| ----------- | --------------------------------------------------------------------------- |
| `--refresh` | Modeller.dev'den model önbelleğini yenileyin                                |
| `--verbose` | Daha ayrıntılı model çıktısı kullanın (maliyetler gibi meta veriler içerir) |

Önbelleğe alınan model listesini güncellemek için `--refresh` bayrağını kullanın. Bu, bir sağlayıcıya yeni modeller eklendiğinde ve bunları aboocode'da görmek istediğinizde kullanışlıdır.

```bash
aboocode models --refresh
```

---

### run

Doğrudan bir istem ileterek aboocode'u etkileşimli olmayan modda çalıştırın.

```bash
aboocode run [message..]
```

Bu, komut dosyası oluşturma, otomasyon veya TUI'un tamamını başlatmadan hızlı bir yanıt istediğinizde kullanışlıdır. Örneğin.

```bash "aboocode run"
aboocode run Explain the use of context in Go
```

Ayrıca, her çalıştırmada MCP sunucusunun soğuk önyükleme sürelerini önlemek için çalışan bir `aboocode serve` örneğine de ekleyebilirsiniz:

```bash
# Start a headless server in one terminal
aboocode serve

# In another terminal, run commands that attach to it
aboocode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Bayraklar

| Bayrak       | Kısa | Açıklama                                                                          |
| ------------ | ---- | --------------------------------------------------------------------------------- |
| `--command`  |      | Çalıştırılacak komut, args için mesajı kullanın                                   |
| `--continue` | `-c` | Son oturuma devam et                                                              |
| `--session`  | `-s` | Devam edecek oturum kimliği                                                       |
| `--fork`     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın)       |
| `--share`    |      | Oturumu paylaşın                                                                  |
| `--model`    | `-m` | provider/model biçiminde kullanılacak model                                       |
| `--agent`    |      | Kullanılacak temsilci                                                             |
| `--file`     | `-f` | Mesaja eklenecek dosya(lar)                                                       |
| `--format`   |      | Biçim: varsayılan (biçimlendirilmiş) veya json (ham JSON olayları)                |
| `--title`    |      | Oturumun başlığı (değer sağlanmazsa kısaltılmış bilgi istemi kullanılır)          |
| `--attach`   |      | Çalışan bir aboocode sunucusuna ekleyin (ör. http://localhost:4096)               |
| `--port`     |      | Yerel sunucunun bağlantı noktası (varsayılan olarak rastgele bağlantı noktasıdır) |

---

### serve

API erişimi için headless bir aboocode sunucusu başlatır. Tam HTTP arayüzü için [server docs](/docs/server) sayfasına bakın.

```bash
aboocode serve
```

Bu, TUI arayüzü olmadan aboocode işlevselliğine API erişimi sağlayan bir HTTP sunucusunu başlatır. HTTP temel kimlik doğrulamasını etkinleştirmek için `ABOOCODE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `aboocode` şeklindedir).

#### Bayraklar

| Bayrak       | Tanım                                      |
| ------------ | ------------------------------------------ |
| `--port`     | Dinlenecek bağlantı noktası                |
| `--hostname` | Dinlenecek ana bilgisayar adı              |
| `--mdns`     | mDNS bulmayı etkinleştir                   |
| `--cors`     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### session

aboocode oturumlarını yönetin.

```bash
aboocode session [command]
```

---

#### list

Tüm aboocode oturumlarını listeleyin.

```bash
aboocode session list
```

##### Bayraklar

| Bayrak        | Kısa | Tanım                                  |
| ------------- | ---- | -------------------------------------- |
| `--max-count` | `-n` | En son N oturumla sınırla              |
| `--format`    |      | Çıkış formatı: tablo veya json (tablo) |

---

### stats

aboocode oturumlarınız için belirteç kullanımı ve maliyet istatistiklerini gösterin.

```bash
aboocode stats
```

#### Bayraklar

| Bayrak      | Açıklama                                                                                                    |
| ----------- | ----------------------------------------------------------------------------------------------------------- |
| `--days`    | Son N güne ait istatistikleri göster (tüm zamanlar)                                                         |
| `--tools`   | Gösterilecek araç sayısı (tümü)                                                                             |
| `--models`  | Model kullanım dökümünü göster (varsayılan olarak gizlidir). En üstteki N'yi göstermek için bir sayı iletin |
| `--project` | Projeye göre filtrele (tüm projeler, boş değer: mevcut proje)                                               |

---

### export

Oturum verilerini JSON olarak dışa aktarın.

```bash
aboocode export [sessionID]
```

Bir oturum kimliği sağlamazsanız mevcut oturumlar arasından seçim yapmanız istenir.

---

### import

Bir JSON dosyasından veya aboocode paylaşımından URL oturum verilerini içe aktarın.

```bash
aboocode import <file>
```

Yerel bir dosyadan veya aboocode paylaşımından (URL) içe aktarabilirsiniz.

```bash
aboocode import session.json
aboocode import https://opncd.ai/s/abc123
```

---

### web

Web arayüzüyle başsız bir aboocode sunucusu başlatın.

```bash
aboocode web
```

Bu, bir HTTP sunucusunu başlatır ve bir web arayüzü aracılığıyla aboocode'a erişmek için bir web tarayıcısı açar. HTTP temel kimlik doğrulamasını etkinleştirmek için `ABOOCODE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `aboocode` şeklindedir).

#### Bayraklar

| Bayrak       | Tanım                                      |
| ------------ | ------------------------------------------ |
| `--port`     | Dinlenecek bağlantı noktası                |
| `--hostname` | Dinlenecek ana bilgisayar adı              |
| `--mdns`     | mDNS bulmayı etkinleştir                   |
| `--cors`     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### acp

Bir ACP (Ajan İstemci Protokolü) sunucusu başlatın.

```bash
aboocode acp
```

Bu komut, nd-JSON kullanarak stdin/stdout aracılığıyla iletişim kuran bir ACP sunucusunu başlatır.

#### Bayraklar

| Bayrak       | Açıklama            |
| ------------ | ------------------- |
| `--cwd`      | Çalışma dizini      |
| `--port`     | Dinlenecek port     |
| `--hostname` | Dinlenecek host adı |

---

### uninstall

aboocode'u kaldırın ve ilgili tüm dosyaları kaldırın.

```bash
aboocode uninstall
```

#### Bayraklar

| Bayrak          | Kısa | Tanım                                           |
| --------------- | ---- | ----------------------------------------------- |
| `--keep-config` | `-c` | Yapılandırma dosyalarını sakla                  |
| `--keep-data`   | `-d` | Oturum verilerini ve anlık görüntüleri saklayın |
| `--dry-run`     |      | Nelerin kaldırılmadan kaldırılacağı göster      |
| `--force`       | `-f` | Onay istemlerini atla                           |

---

### upgrade

aboocode'u en son sürüme veya belirli bir sürüme günceller.

```bash
aboocode upgrade [target]
```

En son sürüme yükseltmek için.

```bash
aboocode upgrade
```

Belirli bir sürüme yükseltmek için:

```bash
aboocode upgrade v0.1.48
```

#### Bayraklar

| Bayrak     | Kısa | Açıklama                                               |
| ---------- | ---- | ------------------------------------------------------ |
| `--method` | `-m` | Kullanılan kurulum yöntemi: curl, npm, pnpm, bun, brew |

---

## Global bayraklar

aboocode CLI aşağıdaki global bayrakları destekler.

| Bayrak         | Kısa | Tanım                                    |
| -------------- | ---- | ---------------------------------------- |
| `--help`       | `-h` | Yardımı görüntüle                        |
| `--version`    | `-v` | Sürüm numarasını yazdır                  |
| `--print-logs` |      | Günlükleri stderr'e yazdır               |
| `--log-level`  |      | Günlük düzeyi (DEBUG, INFO, WARN, ERROR) |

---

## Ortam değişkenleri

aboocode ortam değişkenleri kullanılarak yapılandırılabilir.

| Değişken                              | Tip     | Açıklama                                                                    |
| ------------------------------------- | ------- | --------------------------------------------------------------------------- |
| `ABOOCODE_AUTO_SHARE`                 | boolean | Oturumları otomatik olarak paylaş                                           |
| `ABOOCODE_GIT_BASH_PATH`              | string  | Windows'ta yürütülebilir Git Bash'in Yolu                                   |
| `ABOOCODE_CONFIG`                     | string  | Yapılandırma dosyasının yolu                                                |
| `ABOOCODE_CONFIG_DIR`                 | string  | Yapılandırma dizinine giden yol                                             |
| `ABOOCODE_CONFIG_CONTENT`             | string  | Satır içi JSON config içeriği                                               |
| `ABOOCODE_DISABLE_AUTOUPDATE`         | boolean | Otomatik güncelleme kontrollerini devre dışı bırak                          |
| `ABOOCODE_DISABLE_PRUNE`              | boolean | Eski verilerin temizlenmesini devre dışı bırak                              |
| `ABOOCODE_DISABLE_TERMINAL_TITLE`     | boolean | Otomatik terminal başlığı güncellemelerini devre dışı bırakın               |
| `ABOOCODE_PERMISSION`                 | string  | Satır içi JSON izin config'i                                                |
| `ABOOCODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Varsayılan eklentileri devre dışı bırakın                                   |
| `ABOOCODE_DISABLE_LSP_DOWNLOAD`       | boolean | Otomatik LSP sunucu indirmelerini devre dışı bırakın                        |
| `ABOOCODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Deneysel modelleri etkinleştir                                              |
| `ABOOCODE_DISABLE_AUTOCOMPACT`        | boolean | Otomatik context sıkıştırmayı devre dışı bırak                              |
| `ABOOCODE_DISABLE_CLAUDE_CODE`        | boolean | `.claude`'den okumayı devre dışı bırak (istem + beceriler)                  |
| `ABOOCODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | `~/.claude/CLAUDE.md` dosyasını okumayı devre dışı bırak                    |
| `ABOOCODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | `.claude/skills` yüklemesini devre dışı bırak                               |
| `ABOOCODE_DISABLE_MODELS_FETCH`       | boolean | Uzak kaynaklardan model getirmeyi devre dışı bırakın                        |
| `ABOOCODE_FAKE_VCS`                   | string  | Test amaçlı sahte VCS sağlayıcısı                                           |
| `ABOOCODE_DISABLE_FILETIME_CHECK`     | boolean | Optimizasyon için dosya süresi kontrolünü devre dışı bırakın                |
| `ABOOCODE_CLIENT`                     | string  | Client kimliği (varsayılan: `cli`)                                          |
| `ABOOCODE_ENABLE_EXA`                 | boolean | Exa web arama araçlarını etkinleştir                                        |
| `ABOOCODE_SERVER_PASSWORD`            | string  | `serve`/`web` için temel kimlik doğrulamayı etkinleştirin                   |
| `ABOOCODE_SERVER_USERNAME`            | string  | Temel kimlik doğrulama kullanıcı adını geçersiz kıl (varsayılan `aboocode`) |
| `ABOOCODE_MODELS_URL`                 | string  | Model yapılandırmasını almak için özel URL                                  |

---

### Deneysel

Bu ortam değişkenleri değişebilecek veya kaldırılabilecek deneysel özellikleri etkinleştirir.

| Değişken                                        | Tip     | Tanım                                                   |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `ABOOCODE_EXPERIMENTAL`                         | boolean | Tüm deneysel özellikleri etkinleştir                    |
| `ABOOCODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Simge bulmayı etkinleştir                               |
| `ABOOCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | TUI'da seçim yapıldığında kopyalamayı devre dışı bırak  |
| `ABOOCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | MS cinsinden bash komutları için varsayılan zaman aşımı |
| `ABOOCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | LLM yanıtları için maksimum çıktı belirteçleri          |
| `ABOOCODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Tüm dizin için dosya izleyiciyi etkinleştir             |
| `ABOOCODE_EXPERIMENTAL_OXFMT`                   | boolean | Oxfmt biçimlendiriciyi etkinleştir                      |
| `ABOOCODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Deneysel LSP aracını etkinleştir                        |
| `ABOOCODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Dosya izleyiciyi devre dışı bırak                       |
| `ABOOCODE_EXPERIMENTAL_EXA`                     | boolean | Deneysel Exa özelliklerini etkinleştirin                |
| `ABOOCODE_EXPERIMENTAL_LSP_TY`                  | boolean | Deneysel LSP tür denetimini etkinleştir                 |
| `ABOOCODE_EXPERIMENTAL_MARKDOWN`                | boolean | Deneysel işaretleme özelliklerini etkinleştir           |
| `ABOOCODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Plan modunu etkinleştir                                 |
